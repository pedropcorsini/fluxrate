services:
  db:
    image: postgres:16
    environment:
        POSTGRES_USER: fluxrate
        POSTGRES_PASSWORD: fluxrate
        POSTGRES_DB: fluxrate
    ports:
      - '5432:5432'
    volumes:
    - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
